defmodule Echo.Chats.ChatRegistry do
  # Es un Registry que guarda los ChatSession.
  # Así cuando un usuario hace una request sobre un chat,
  # se usa la Chatsession correspondiente al chat en cuestión (si no hay la crea)
end
